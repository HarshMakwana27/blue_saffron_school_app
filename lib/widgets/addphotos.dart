import 'package:flutter/material.dart';

class AddPhotosScreen extends StatelessWidget {
  const AddPhotosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('Add photos'),
    );
  }
}
